# =============================================================================
# Wolfpack AI - 后端依赖
# =============================================================================
# 安装:
#   source backend/venv/bin/activate
#   pip install -r backend/requirements.txt
# =============================================================================

# ---------- 通用 ----------
python-dotenv>=1.0.1
pydantic>=2.0
pydantic-settings>=2.13.1
loguru>=0.7.2

# ---------- Web 服务 (FastAPI + uvicorn) ----------
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# ---------- 持久化 (SQLAlchemy 2.0 async + SQLite + alembic) ----------
sqlalchemy[asyncio]>=2.0.46
aiosqlite>=0.22.1
alembic>=1.18.4

# ---------- LangChain / LangGraph (主框架) ----------
langchain>=1.2.13
langchain-anthropic>=0.3.0          # 走 Novita Anthropic 端点 (默认)
langgraph>=0.3.0
